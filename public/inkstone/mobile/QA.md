# Inkstone Mobile 原型检查记录

2026-09-14（Gemini 增量改造）。检查对象为重构后的 `mobile/` 源码与生成的 `../proto-08-mobile-gemini.html`，对齐桌面端侧栏分页、右侧面板、知识中心、子代理、权限等结构。
2026-09-05。检查对象为本目录原型与生成的 `../proto-08-mobile.html`，不表示产品功能已实现或经过真机认证。

## 静态与项目检查

- `pnpm typecheck`：通过。根脚本按项目既有定义排除 `@piwin/mobile`；本次仅增加设计原型，无应用 TypeScript 改动。
- 五份原型 JavaScript 的语法检查：通过。
- 单文件生成后的五段内联脚本语法检查：通过。
- 16 个主页面、62 个底部抽屉的首轮模板输出检查：无 `undefined`、无空输出。
- 321 个模板控件引用与 69 个控制器动作核对：未发现缺失动作、缺失页面或缺失抽屉。
- 自包含文件没有外部脚本、样式或字体依赖；内容和媒体均为模拟，未访问 Host、麦克风、相机或私人资产。
- 原型源按主题、布局、页面、检视器、抽屉、交互分开；所有新增源码和生成文件均低于 1000 行。

## 浏览器检查

使用本地预览验证了可编辑版本与生成的自包含交付件。

| 检查 | 结果 |
|---|---|
| 桌面展台、侧边场景目录、纸墨切换 | 通过；矮窗口缩小展台留白，主导航与主题入口保持可见 |
| 390 × 844 手机布局 | 对话正文独立滚动，砚台固定于下方，无桌面外框 |
| 360 × 780 墨面审阅 | 页面宽度与滚动宽度同为 360；代码在局部容器横向滚动 |
| 模型、思考强度 | 选择即时呈现在抽屉与砚台；历史助手署名固定于原模型 |
| 消息输入与排队 | 工作中输入后主键切换排队，点击生成队列条 |
| 审批印章 | 允许请求后变为已处理状态；选择范围可在完整请求中操作 |
| 知识卡片 | 翻面出现四档评分，评分后进入下一张；浏览与复习分开计数 |
| 变更批注 | 点击第 42 行、填写批注、保存后显示在差异下方并关联主会话 |
| 工作区与终端 | 终端呈现只读快照；输入下一条命令后回到砚台草稿 |
| 断线与重连 | 发送被拦截；草稿可修改；重连后草稿保留且未自动发送 |
| 新建会话 | 显示新消息与空白转录，不混入示例历史 |
| 语音转文字与 Live | 转录可编辑；进入 Live 后可静音、结束并返回当前会话 |
| 案头主机卡、四格资料架与分类设置 | 通过；主机卡片文字分行清晰、印字居中；资料架 2×2 博古格无重叠；13 项设置对应专属矢量图标 |
| 浏览器运行日志 | 已检查的交付件交互过程中未发现 error / warn 日志 |

## 适用边界

这是设计原型。Host 连接、权限、队列、生成、FSRS 调度、资产、OAuth、插件安装和通知均不接真实服务。原型偏好和草稿只在当前页面实例中保留，刷新后重置。尚未验证 iOS / Android 真机键盘、系统安全区、辅助阅读器、实际网络重连或真实后端协议。实际产品实施时，组件应使用公共 `@piwin/ui-kit` 能力与 Host 合同。
